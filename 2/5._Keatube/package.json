{
  "name": "keatube",
  "version": "1.0.0",
  "description": "A video site",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env PORT=80 node app.js",
    "start-dev": "cross-env PORT=3000 nodemon app.js",
    "start-prod": "sudo PORT=80 pm2 start app.js -f"
  },
  "keywords": [
    "video"
  ],
  "author": "Wajid",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "install": "^0.13.0",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
